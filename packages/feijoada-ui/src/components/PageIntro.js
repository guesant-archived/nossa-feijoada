import * as React from "react";

const PageIntro = ({ children }) => (
  <div
    className="tw-container tw-mx-auto tw-px-4 tw-flex tw-flex-col tw-justify-center tw-items-center"
  >
    {children}
  </div>
);

export default PageIntro;
